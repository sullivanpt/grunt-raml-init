#%RAML 0.8

displayName: People

get:
  responses:
    200:
      body:
        application/json:
          schema: !include ./schema.json
          example: !include ./sample.json

/{personId}: !include ../item/index.raml
